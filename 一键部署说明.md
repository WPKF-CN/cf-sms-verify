# 一键部署（GitHub + Deploy to Cloudflare 按钮）

## 本项目的部署按钮

仓库地址：https://github.com/WPKF-CN/cf-sms-verify

**按钮链接**（Worker 在 `worker/` 子目录，所以链接要带 `/tree/main/worker`）：

```
https://deploy.workers.cloudflare.com/?url=https://github.com/WPKF-CN/cf-sms-verify/tree/main/worker
```

**Markdown 写法**（可直接贴到 README、网站、聊天窗口）：

```markdown
[![Deploy to Cloudflare](https://deploy.workers.cloudflare.com/button)](https://deploy.workers.cloudflare.com/?url=https://github.com/WPKF-CN/cf-sms-verify/tree/main/worker)
```

**HTML 写法**：

```html
<a href="https://deploy.workers.cloudflare.com/?url=https://github.com/WPKF-CN/cf-sms-verify/tree/main/worker">
  <img src="https://deploy.workers.cloudflare.com/button" alt="Deploy to Cloudflare"/>
</a>
```

> 已实测：点击后 Cloudflare 能正确识别仓库与子目录，跳转到部署配置页。

---

这是**交付给客户最省事**的方式：客户点一个按钮，Cloudflare 自动完成
克隆仓库、建 D1 数据库、建表、构建、部署、绑定，全程不需要命令行。

```
客户点击按钮 → 授权 GitHub → 授权 Cloudflare → 填仓库名/数据库名 → 完成
```

---

## 一、原理：按钮会做什么

Cloudflare 官方文档（[Deploy to Cloudflare buttons](https://developers.cloudflare.com/workers/platform/deploy-buttons/)）说明，按钮会：

1. **克隆仓库**到用户自己的 GitHub 账号（生成一个新仓库，之后可以继续改）
2. **读取 `wrangler.toml`** 判断需要哪些资源
3. **自动创建所需资源**——D1 数据库会自动创建，并**把新的 database_id 自动回填**，不需要手工复制 ID
4. **执行 `predeploy` 脚本**（我们用它自动建表）
5. **构建并部署**，绑定全部自动完成

> **本项目部署时不需要填写任何密钥。** 后台账号密码在部署完成后首次访问 `/admin` 时创建；
> 短信商密钥和 Turnstile Secret 都在后台左侧菜单里填写（加密存入你自己的 D1）。
> 这样客户在 Cloudflare 的部署弹窗里只需要点几下「下一步」。

关键限制（官方文档明确列出）：

| 项目 | 要求 |
|---|---|
| 仓库可见性 | **必须公开**（私有仓库无法使用按钮） |
| 平台 | 只支持 GitHub / GitLab.com，不支持自建 GitLab |
| 类型 | 只支持 Workers 应用，不支持 Pages |
| 目录结构 | 不支持 monorepo 混合，若用子目录则必须完全自包含 |

---

## 二、本项目已经为此改造完成

为了让按钮能一键跑通，仓库里已经准备好了这几样东西：

| 文件 | 作用 |
|---|---|
| `worker/wrangler.toml` | 声明了 `DB` 绑定，`database_id` 用占位值（Cloudflare 部署时自动替换成真实 ID） |
| `worker/migrations/0001_init.sql` | 建表脚本（config / verifications / rate 三张表） |
| `worker/package.json` 的 `predeploy` | `wrangler d1 migrations apply DB --remote`，部署前自动建表 |
| `worker/migrations/0002_secrets_and_code.sql` | 凭证表 + 管理员表 + 自建验证码字段 |
| `worker/src/lib/secrets.js` | 凭证的加密存储（AES-GCM） |

> `predeploy` 用的是**绑定名 `DB`** 而不是数据库名，这样即使客户把数据库改名也能正常建表。
> 迁移脚本带 `IF NOT EXISTS`，重复部署不会出错（已实测：第二次显示 `No migrations to apply`）。

---

## 三、代码已经在 GitHub 上了

仓库：https://github.com/WPKF-CN/cf-sms-verify （公开仓库，共 27 个文件）

已包含部署所需的全部内容：

| 文件 | 作用 |
|---|---|
| `worker/wrangler.toml` | 声明 `DB` 绑定，`database_id` 用占位 UUID（Cloudflare 部署时自动替换） |
| `worker/migrations/0001_init.sql` | 建表脚本 |
| `worker/package.json` | `predeploy` 自动建表 + 9 个密钥的中文说明 |
| `worker/.dev.vars.example` | 声明需要哪些密钥 |

后续要改代码：本地改完 → `git add -A && git commit -m "说明" && git push` 即可。

---

## 四、客户那边的体验

客户点击按钮后，会经历：

1. **授权 GitHub**——Cloudflare 把仓库复制到客户账号下
2. **填写配置**——页面只会问两项：
   - 仓库名
   - **D1 数据库名称**（默认 `sms-verify`，可改）

   **没有任何密钥需要填。**

3. **点 Deploy**——Cloudflare 自动建库、建表、构建、部署、绑定
4. 部署完成，客户拿到 `https://<worker名>.<账号>.workers.dev`
5. 打开 `<地址>/admin`，**首次访问会引导创建管理员账号**（自己设账号和密码即可）
6. 登录后进左侧菜单，在「短信通道」「人机检测」两页填密钥（加密存进客户自己的 D1）

**客户唯一需要提前准备的**：一个短信平台账号（Twilio 或 Onbuka）。
其余（D1 数据库、绑定、建表、后台账号）全自动。

---

## 五、部署完成后，客户还要做两件事

这两件事没法自动化，需要在我们的**管理后台**里点（因为存在 D1 里、可以随时改）：

1. 打开 `https://<worker名>.<账号>.workers.dev/admin`，首次访问设置账号密码，之后用它登录
2. 左侧菜单里依次填几个页面（每页填完点右上角**保存修改**）：
   - 「短信通道」→ 填**通道凭证**（Twilio 或 Onbuka 的密钥）
   - 「国家与频控」→ 勾选**允许的国家**（只勾美国 = 只允许美国号码）
   - 「人机检测」→ 填 **Site Key** 和 **Secret Key**，并把开关打开（之前不用准备）
   - 「跳转与站点」→ 把**允许的站点来源**从 `*` 改成客户自己的落地页域名

3. 最后把 Worker 地址填进落地页的 `apiBase`，就接入完成了

---

## 六、和另外两种部署方式的对比

| 方式 | 适合场景 | 客户需要会什么 |
|---|---|---|
| **Deploy to Cloudflare 按钮** | 交付给客户、或公开分享 | 会点按钮、会注册 Twilio |
| **手动网页部署**（见 [手动部署指南.md](./手动部署指南.md)） | 客户不想连 GitHub、或要精细控制 | 会复制粘贴、会找控制台菜单 |
| **命令行**（`npm run setup` / `secrets` / `deploy`） | 自己用、或批量给多个客户部署 | 会用终端 |

三种方式部署出来的东西完全一样。

---

## 七、常见问题

**Q：仓库必须公开吗？**
是的，这是官方限制。如果你的代码不想公开，就用「手动部署指南」那种方式，或者让客户 fork 私有仓库后手动配置 Workers Builds。

**Q：客户点按钮后，代码在谁那儿？**
在客户自己的 GitHub 账号下（Cloudflare 会自动创建一个新仓库）。这也是这个方式的好处——交付干净，后续客户能自己改。

**Q：能自动创建 Twilio 账号吗？**
不能。Twilio 账号需要客户自己注册并做实名/风控审核，这一步任何工具都替代不了。

**Q：以后我更新了代码，客户怎么拿到？**
按钮方式是一次性克隆，不会自动同步。要更新的话，让客户在自己仓库里拉取你的改动，或者在 Cloudflare 里配置 Workers Builds 关联你的上游仓库。

**Q：D1 数据库在客户的账号里还是我的？**
在**客户的账号**里，账单和数据都属于客户。这对交付来说是正确的。

**Q：部署失败怎么办？**
Cloudflare 部署页面会显示构建日志。最常见的两个原因：① 密钥没填（比如 Twilio 三项留空）；② 仓库结构不符合要求（子目录不自包含）。
