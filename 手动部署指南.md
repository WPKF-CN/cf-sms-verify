# 手动部署指南（纯 Cloudflare 网页后台，不用命令行）

适合：不想装命令行工具、或要给客户在自己账号里部署的场景。
全程在浏览器里完成，不需要 wrangler、不需要登录授权弹窗。

总耗时约 15 分钟，一共 6 步：

1. 生成密码哈希
2. 建 D1 数据库
3. 建表（跑一段 SQL）
4. 创建 Worker 并粘贴代码
5. 绑定数据库 + 配置密钥
6. 验证并接入落地页

---

## 准备：拿到单文件代码

在项目目录执行一次（只需一次）：

```bash
cd sms-verify/worker
npm install
npm run bundle
```

会生成 **`sms-verify/worker/dist/worker.js`**（约 272 KB）。
用文本编辑器（VS Code / 备忘录 / 文本编辑都行）打开它，**全选、复制**，第 4 步要粘贴。

> 为什么需要打包：源码分成多个文件，网页后台的编辑器只能贴一个文件，所以先合并成一个。
> 改了源码之后重新跑 `npm run bundle` 即可。

---

## 第 1 步：生成后台密码的哈希

Cloudflare 里只能存密码的哈希（不能存明文）。两种方式任选：

**方式 A（推荐）**：在项目目录执行

```bash
npm run hash -- 你的密码
```

会输出一串 64 位字符，例如：

```
240be518fabd2724ddb6f04eeb1da5967448d7e831c08c8fa822809f74c720a9
```

**方式 B（纯网页）**：打开 https://emn178.github.io/online-tools/sha256.html
在输入框里输入你的后台密码，下面 `SHA-256` 那行结果就是哈希。

> 把这串哈希复制下来，第 5 步要用。
> 注意：**不要**把明文密码填进 Cloudflare，填哈希。

---

## 第 2 步：创建 D1 数据库

1. 登录 https://dash.cloudflare.com
2. 左侧菜单点 **Workers & Pages**（有些账号显示为 **Compute (Workers)**）
3. 在这个页面里点 **D1 SQL database**
   直达链接：https://dash.cloudflare.com/?to=/:account/workers/d1
4. 点 **Create Database**
5. 名字填：`sms-verify`
6. 点 **Create**

创建完进入这个数据库的详情页，**把页面上那串 `Database ID`（形如 `a1b2c3d4-1111-2222-3333-444455556666`）复制下来**，第 5 步要用。

---

## 第 3 步：建表（执行 SQL）

还在刚才那个数据库页面里：

1. 点顶部标签 **Console**
2. 打开项目里的 `sms-verify/worker/schema.sql`，**全选、复制**全部内容
3. 粘贴到 Console 的输入框里
4. 点 **Execute**

执行成功会显示几条 `success`。这一步建了 3 张表：

| 表名 | 用途 |
|---|---|
| `config` | 后台里改的配置（允许的国家、频控、文案等） |
| `verifications` | 验证记录（后台「记录」页的数据来源） |
| `rate` | 频控计数（限流用） |

> 重复执行也安全，建表语句都带了 `IF NOT EXISTS`。

---

## 第 4 步：创建 Worker 并粘贴代码

1. 左侧菜单点 **Workers & Pages**
   直达链接：https://dash.cloudflare.com/?to=/:account/workers-and-pages
2. 点 **Create application** → 选 **Start with Hello World!** → **Get started**
3. 名字填：`sms-verify`（这个名字决定你的访问地址）
4. 点 **Deploy**（先把模板部署上去）
5. 部署完成后点 **Edit code**（或先 **Continue to project**，再点右上角 **Edit code**）
6. 在左侧编辑器里 **全选（Cmd+A）→ 删除 → 粘贴** 你第 0 步复制的 `worker.js` 全部内容
7. 右上角点 **Deploy**

部署成功后会给你一个地址，形如：

```
https://sms-verify.<你的账号子域>.workers.dev
```

**先别急着用**，第 5 步配置完才能真正跑起来。

> 如果粘贴后编辑器提示代码过大：Cloudflare 免费版单个 Worker 上限是 3 MB（压缩后），我们这个只有 272 KB，没问题。

---

## 第 5 步：绑定数据库 + 配置密钥（最关键的一步）

回到这个 Worker 的详情页。注意：**绑定**和**变量**在两个不同的位置，别找错了。

### 5.1 绑定 D1 数据库

1. 在 Worker 详情页点顶部的 **Bindings** 标签（不是 Settings 里）
2. 点 **Add binding**
3. 类型选 **D1 database** → **Add binding**
4. **Variable name 必须填 `DB`**（大写，不能改，代码里写死了）
5. D1 database 下拉框选第 2 步建的 `sms-verify`
6. 点 **Add binding** 保存

> ⚠️ 变量名填错（比如填成 `db` 或 `DATABASE`）会直接报错，这是最常见的坑。

### 5.2 添加环境变量和密钥

1. 点顶部的 **Settings** 标签
2. 找到 **Variables and Secrets** → 点 **Add**
3. 先加一条普通变量：**Type 选 `Text`**，变量名 `MOCK_MODE`，值填 `0`

| 变量名 | 值 | 类型 | 说明 |
|---|---|---|---|
| `MOCK_MODE` | `0` | Text | 填 0 才是真发短信；填 1 是演示模式（不发短信，验证码固定 123456） |

4. 再逐个添加密钥：点 **Add** → **Type 选 `Secret`**（加密，保存后看不到明文）

| 密钥名 | 值 | 必填 |
|---|---|---|
| `ADMIN_PASSWORD_HASH` | 第 1 步算出来的哈希 | ✅ 必填（后台登录用） |
| `SESSION_SECRET` | 随便一串长随机字符，例如 `a8f3k2m9x7q1w5e4r6t0y8u2i3o4p5` | ✅ 必填 |
| `TURNSTILE_SECRET` | Cloudflare Turnstile 的 Secret Key（见下方说明） | 建议填 |
| `TWILIO_ACCOUNT_SID` | Twilio 的 Account SID（`AC` 开头） | 发短信必填 |
| `TWILIO_AUTH_TOKEN` | Twilio 的 Auth Token | 发短信必填 |
| `TWILIO_VERIFY_SERVICE_SID` | Twilio Verify 服务的 SID（`VA` 开头） | 发短信必填 |
| `PLIVO_AUTH_ID` | Plivo 的 Auth ID（如果开通了 Plivo） | 可选 |
| `PLIVO_AUTH_TOKEN` | Plivo 的 Auth Token | 可选 |
| `PLIVO_APP_UUID` | Plivo Verify 应用的 UUID | 可选 |

5. 加完后点页面底部的 **Deploy**，让配置生效

> 一次可以点 **Add variable** 连续加多条，不用每加一条就保存一次；全部加完统一 Deploy。

### Turnstile 密钥怎么拿（人机检测）

1. Cloudflare 控制台左侧找 **Turnstile**
2. **Add widget** → Widget name 随便填（如 `sms-verify`）
3. **Hostnames** 填你所有落地页的域名（例如 `lander-a.com`、`www.lander-a.com`），**这是防止别人盗用你短信额度的关键**
4. Widget Mode 选 **Managed**
5. 创建后页面会给两个值：
   - **Site Key**（公开的）→ 记下来，第 6 步要填到后台
   - **Secret Key**（保密的）→ 填到上面的 `TURNSTILE_SECRET`

---

## 第 6 步：验证 + 接入落地页

### 6.1 验证部署

浏览器依次打开（把域名换成你的）：

1. `https://sms-verify.<账号>.workers.dev/admin`
   → 应该出现登录页，输入你第 1 步设的密码能进去
2. 进后台后点 **测试** 标签 → 填自己的手机号 → 点发送
   → 正常的话手机会收到验证码，页面显示走了哪个通道
3. 打开 `https://sms-verify.<账号>.workers.dev/demo`
   → 点 Join Group，走一遍完整流程

### 6.2 在后台做三项配置

登录后台 → **配置** 标签：

1. **允许的国家**：勾选你要放行的国家（只勾美国 = 只允许美国号码）
2. **人机检测**：把第 5 步拿到的 **Site Key** 填进去，启用选「启用」
3. **允许的站点来源**：把 `*` 改成你的落地页域名，每行一个

改完点最下面的 **保存配置**，约 10 秒后生效。

### 6.3 接入落地页

打开你的落地页 `ai/index.html`，找到文件末尾的这段：

```html
window.SMS_VERIFY_CONFIG = {
    apiBase: '',                 // ← 把这里改成你的 Worker 地址
    site: 'ai-lander',
    failOpen: true,
};
```

把 `apiBase` 填成 `'https://sms-verify.<你的账号>.workers.dev'`（注意引号要保留），保存上传即可。

其他落地页同样处理，只改 `apiBase`，`site` 改成不同的名字方便在后台区分来源。

---

## 常见问题

**Q：后台打不开 / 一直转圈**
多半是 `workers.dev` 在国内被墙。给 Worker 绑一个自己的域名：Worker 详情页 → **Settings** → **Domains & Routes** → **Add** → **Custom Domain**，填 `sms.你的域名.com`（域名需已托管在 Cloudflare）。然后用新域名访问。

**Q：发验证码提示失败 / 收不到短信**
按顺序查：① `TWILIO_*` 三个密钥是否都填了；② Twilio 是试用账号的话，收信号码必须先加到 Twilio 后台的 **Verified Caller IDs**；③ 后台「测试」页会显示具体错误原因，照那个查。

**Q：提示「国家不支持」**
后台「配置」里勾选的国家没有包含该号码。注意美国和加拿大都是 +1，系统是按号码段区分的，勾了美国不代表加拿大能过。

**Q：想先不花短信费试一下**
把变量 `MOCK_MODE` 改成 `1` 再部署，此时不会真发短信，验证码固定是 `123456`。测完记得改回 `0`。

**Q：改了配置要重新部署吗？**
不用。后台「配置」页改的所有参数都存在 D1 里，保存后约 10 秒对所有落地页生效。

**Q：密钥填错了怎么改？**
Worker → **Settings** → **Variables and Secrets**，点对应项编辑，保存后再点一次 **Deploy**。

**Q：Worker 代码想更新怎么办？**
本地改完源码后重新执行 `npm run bundle`，再回到 Worker → **Edit code** → 全选覆盖粘贴 → **Deploy**。

---

## 给客户部署时的建议

如果这套东西要交付到**客户的 Cloudflare 账号**里：

1. **让客户建账号并完成验证**，把账号权限给你（推荐用 Cloudflare 的 **Members** 功能邀请你为成员，而不是共用账号密码）
2. 上面 6 步全部在**客户的账号**里操作，D1、Worker、Twilio 都归属客户
3. **短信账号（Twilio）建议也开在客户名下**，账单和合规责任清晰，避免以后扯皮
4. 后台密码让客户自己设，你只帮他算哈希、不要留明文
5. 交付时告诉客户三件事：后台地址、如何改允许的国家、如何看记录和导出

如果你要给多个客户批量部署，用命令行会快得多（`npm run setup` + `npm run secrets` + `npm run deploy`），
配合 Cloudflare **API Token** 就能免去每次点浏览器授权。需要的话我可以把这套脚本补上。
